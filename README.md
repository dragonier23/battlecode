# Cambridge Battlecode 2026

Team Ash Hit

# Create a new bot
Run this script to create a new bot. This script initialises the directory with shared utility functions.

`perl mkbot.pl`

# Update the bot with new version of common code
Run

`perl sync.pl <bot-name>`